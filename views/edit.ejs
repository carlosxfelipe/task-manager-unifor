<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <title>Editar Tarefa</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <div class="container">
      <h1>Editar Tarefa</h1>
      <form action="/edit/<%= tarefa.tarefa_id %>" method="POST">
        <label for="titulo">Título:</label>
        <input
          type="text"
          name="titulo"
          value="<%= tarefa.titulo %>"
          required
        />

        <label for="descricao">Descrição:</label>
        <textarea name="descricao" rows="5"><%= tarefa.descricao %></textarea>

        <button type="submit">Salvar</button>
      </form>
    </div>
  </body>
</html>
